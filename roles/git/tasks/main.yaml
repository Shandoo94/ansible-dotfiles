- name: Copy file configurations
  ansible.builtin.copy:
    src: "{{ item }}"
    dest: "{{ git_source_dir }}/{{ item }}"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: "0600"
  loop:
    - config
  tags: [git, config]
