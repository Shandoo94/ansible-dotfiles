- name: Create source directory
  ansible.builtin.file:
    dest: "{{ zsh_source_dir }}"
    state: directory
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0700"
  tags: [zsh, config]

- name: Render template configurations
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: "{{ zsh_source_dir }}/.{{ item }}"
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0600"
  loop:
    - zshenv
    - zshrc
  tags: [zsh, config]

- name: Determine oh-my-posh target platform
  ansible.builtin.set_fact:
    zsh_omp_platform: "{{ {'Linux': 'linux', 'Darwin': 'darwin', 'FreeBSD': 'freebsd'}.get(ansible_facts['system'], ansible_facts['system'] | lower) }}"
    zsh_omp_arch: "{{ {'x86_64': 'amd64', 'amd64': 'amd64', 'aarch64': 'arm64', 'arm64': 'arm64', 'armv7l': 'arm', 'armv7': 'arm', 'armv6l': 'arm', 'armv6': 'arm', 'armhf': 'arm'}.get(ansible_facts['userspace_architecture'], ansible_facts['userspace_architecture'] | lower) }}"
  tags: [zsh, oh-my-posh]

- name: Validate oh-my-posh target support
  ansible.builtin.assert:
    that:
      - zsh_omp_platform in ['linux', 'darwin', 'freebsd']
      - zsh_omp_arch in ['amd64', 'arm', 'arm64']
    fail_msg: "Unsupported platform {{ ansible_facts['userspace_architecture'] }} or architecture {{ ansible_facts['system'] }} for oh-my-posh"
  tags: [zsh, oh-my-posh]

- name: Define oh-my-posh asset details
  ansible.builtin.set_fact:
    zsh_omp_asset_filename: "posh-{{ zsh_omp_platform }}-{{ zsh_omp_arch }}"
    zsh_omp_checksums_url: "https://github.com/JanDeDobbeleer/oh-my-posh/releases/download/{{ zsh_omp_release_version }}/checksums.txt"
    zsh_omp_asset_url: "https://github.com/JanDeDobbeleer/oh-my-posh/releases/download/{{ zsh_omp_release_version }}/posh-{{ zsh_omp_platform }}-{{ zsh_omp_arch }}"
  tags: [zsh, oh-my-posh]

- name: Create oh-my-posh binary directory
  ansible.builtin.file:
    path: "{{ zsh_omp_bin_dir }}"
    state: directory
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0755"
  tags: [zsh, oh-my-posh]

- name: Create oh-my-posh source directory
  ansible.builtin.file:
    dest: "{{ zsh_omp_source_dir }}"
    state: directory
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0700"
  tags: [zsh, config]

- name: Fetch oh-my-posh checksums
  ansible.builtin.uri:
    url: "{{ zsh_omp_checksums_url }}"
    return_content: true
  register: zsh_omp_checksums
  tags: [zsh, oh-my-posh]

- name: Extract oh-my-posh checksum
  ansible.builtin.set_fact:
    # regex_findall returns a list of matches.
    # Since the regex has a capture group (), it returns a list of the captured strings.
    # We use | first to get the string from that list.
    zsh_omp_checksum: "{{ zsh_omp_checksums.content | regex_findall('^([0-9a-f]{64})\\s+' ~ zsh_omp_asset_filename ~ '$', multiline=True) | first }}"
  tags: [zsh, oh-my-posh]

- name: Ensure oh-my-posh checksum was located
  ansible.builtin.assert:
    that:
      - zsh_omp_checksum is string
      - zsh_omp_checksum | length == 64
    fail_msg: "Unable to find checksum for {{ zsh_omp_asset_filename }}"
  tags: [zsh, oh-my-posh]

- name: Download oh-my-posh binary
  ansible.builtin.get_url:
    url: "{{ zsh_omp_asset_url }}"
    dest: "{{ zsh_omp_bin_dir }}/oh-my-posh"
    checksum: "sha256:{{ zsh_omp_checksum }}"
    mode: "0755"
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
  tags: [zsh, oh-my-posh]

- name: Copy oh-my-posh config
  ansible.builtin.copy:
    src: omp_config.yaml
    dest: "{{ zsh_omp_source_dir }}/config.yaml"
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0600"
  tags: [zsh, oh-my-posh, config]

- name: Render template configurations
  ansible.builtin.template:
    src: zshenv_home.j2
    dest: "{{ ansible_facts['user_dir'] }}/.zshenv"
    owner: "{{ ansible_facts['user_id'] }}"
    group: "{{ ansible_facts['user_id'] }}"
    mode: "0600"
  tags: [zsh, config]
